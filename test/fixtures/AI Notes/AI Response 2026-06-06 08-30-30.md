
---

Here is the response you asked for:

```cpp
int main() {
    std::cout << "Hello, World";
    return 0;
}
```

That concludes the AI response. Note this file opens with a blank line and a
stray `---` and never closes it — the exact shape that crashed Quartz's YAML
parser. mdsite must build it without error.
